import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zizotrial.egyptianprince',
  appName: ' egyptian prince',
  webDir: 'dist'
};

export default config;
